# API Configuration
# URL base do backend Django
NEXT_PUBLIC_API_URL=http://localhost:8000

# Site Configuration
# URL base do site frontend (usado para SEO, sitemap, robots.txt, etc.)
# Em produção, configure com a URL real do site (ex: https://upai.com)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Environment
# Não precisa configurar manualmente, é definido automaticamente pelo Next.js
# NODE_ENV=development
